/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "source.unsplash.com",
      "images.unsplash.com",
      "static.independent.co.uk",
      "static.vecteezy.com",
    ],
  },
};

export default nextConfig;
